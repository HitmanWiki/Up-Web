export const ConfigStatusEnum = {
  Initialization: "initialization",
  Loading: "loading",
  Preparing: "preparing",
}

export enum ConfigStatusEnumV2 {
  Initialization = "initialization",
  Loading = "loading",
  Preparing = "preparing",
}
