import styled from "styled-components"

export const Wrapper = styled.div`
  input {
    font-weight: 400;
    font-size: 16px;
    line-height: 24px;

    ::placeholder {
      color: var(--theme-search-color);
    }
  }
`
